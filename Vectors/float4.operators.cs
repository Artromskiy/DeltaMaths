// <auto-generated />
#nullable enable
using System;
using System.Runtime.InteropServices;
using System.Runtime.CompilerServices;

namespace DVG.Maths
{
    public partial struct float4
    {

        public static bool operator ==(float4 left, float4 right)
        {
            return left.x == right.x && left.y == right.y && left.z == right.z && left.w == right.w;
        }

        public static bool operator !=(float4 left, float4 right)
        {
            return left.x != right.x || left.y != right.y || left.z != right.z || left.w != right.w;
        }

        public static float4 operator +(float4 value)
        {
            return new(+value.x, +value.y, +value.z, +value.w);
        }

        public static float4 operator -(float4 value)
        {
            return new(-value.x, -value.y, -value.z, -value.w);
        }

        public static float4 operator ++(float4 value)
        {
            return new(++value.x, ++value.y, ++value.z, ++value.w);
        }

        public static float4 operator --(float4 value)
        {
            return new(--value.x, --value.y, --value.z, --value.w);
        }

        public static float4 operator +(float4 left, float4 right)
        {
            return new(left.x + right.x, left.y + right.y, left.z + right.z, left.w + right.w);
        }

        public static float4 operator -(float4 left, float4 right)
        {
            return new(left.x - right.x, left.y - right.y, left.z - right.z, left.w - right.w);
        }

        public static float4 operator *(float4 left, float4 right)
        {
            return new(left.x * right.x, left.y * right.y, left.z * right.z, left.w * right.w);
        }

        public static float4 operator /(float4 left, float4 right)
        {
            return new(left.x / right.x, left.y / right.y, left.z / right.z, left.w / right.w);
        }

        public static float4 operator +(float4 left, float right)
        {
            return new(left.x + right, left.y + right, left.z + right, left.w + right);
        }

        public static float4 operator +(float left, float4 right)
        {
            return new(left + right.x, left + right.y, left + right.z, left + right.w);
        }

        public static float4 operator -(float4 left, float right)
        {
            return new(left.x - right, left.y - right, left.z - right, left.w - right);
        }

        public static float4 operator -(float left, float4 right)
        {
            return new(left - right.x, left - right.y, left - right.z, left - right.w);
        }

        public static float4 operator *(float4 left, float right)
        {
            return new(left.x * right, left.y * right, left.z * right, left.w * right);
        }

        public static float4 operator *(float left, float4 right)
        {
            return new(left * right.x, left * right.y, left * right.z, left * right.w);
        }

        public static float4 operator /(float4 left, float right)
        {
            return new(left.x / right, left.y / right, left.z / right, left.w / right);
        }

        public static float4 operator /(float left, float4 right)
        {
            return new(left / right.x, left / right.y, left / right.z, left / right.w);
        }

        public static float4 operator %(float4 left, float4 right)
        {
            return new(left.x % right.x, left.y % right.y, left.z % right.z, left.w % right.w);
        }

        public static float4 operator %(float4 left, float right)
        {
            return new(left.x % right, left.y % right, left.z % right, left.w % right);
        }

        public static float4 operator %(float left, float4 right)
        {
            return new(left % right.x, left % right.y, left % right.z, left % right.w);
        }

        public static bool4 operator <(float4 left, float4 right)
        {
            return new(left.x < right.x, left.y < right.y, left.z < right.z, left.w < right.w);
        }

        public static bool4 operator <(float4 left, float right)
        {
            return new(left.x < right, left.y < right, left.z < right, left.w < right);
        }

        public static bool4 operator <(float left, float4 right)
        {
            return new(left < right.x, left < right.y, left < right.z, left < right.w);
        }

        public static bool4 operator <=(float4 left, float4 right)
        {
            return new(left.x <= right.x, left.y <= right.y, left.z <= right.z, left.w <= right.w);
        }

        public static bool4 operator <=(float4 left, float right)
        {
            return new(left.x <= right, left.y <= right, left.z <= right, left.w <= right);
        }

        public static bool4 operator <=(float left, float4 right)
        {
            return new(left <= right.x, left <= right.y, left <= right.z, left <= right.w);
        }

        public static bool4 operator >(float4 left, float4 right)
        {
            return new(left.x > right.x, left.y > right.y, left.z > right.z, left.w > right.w);
        }

        public static bool4 operator >(float4 left, float right)
        {
            return new(left.x > right, left.y > right, left.z > right, left.w > right);
        }

        public static bool4 operator >(float left, float4 right)
        {
            return new(left > right.x, left > right.y, left > right.z, left > right.w);
        }

        public static bool4 operator >=(float4 left, float4 right)
        {
            return new(left.x >= right.x, left.y >= right.y, left.z >= right.z, left.w >= right.w);
        }

        public static bool4 operator >=(float4 left, float right)
        {
            return new(left.x >= right, left.y >= right, left.z >= right, left.w >= right);
        }

        public static bool4 operator >=(float left, float4 right)
        {
            return new(left >= right.x, left >= right.y, left >= right.z, left >= right.w);
        }

        public static implicit operator double4(float4 value)
        {
            return new double4((double)value.x, (double)value.y, (double)value.z, (double)value.w);
        }

        public static explicit operator int4(float4 value)
        {
            return new int4((int)value.x, (int)value.y, (int)value.z, (int)value.w);
        }

        public static explicit operator uint4(float4 value)
        {
            return new uint4((uint)value.x, (uint)value.y, (uint)value.z, (uint)value.w);
        }

        public static explicit operator fix4(float4 value)
        {
            return new fix4((fix)value.x, (fix)value.y, (fix)value.z, (fix)value.w);
        }
    }
}
