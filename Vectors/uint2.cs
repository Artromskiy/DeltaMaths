// <auto-generated />
#nullable enable
using System;
using System.Runtime.InteropServices;
using System.Runtime.CompilerServices;

namespace DVG.Maths
{
    /// <summary>A vector of type uint with 2 components.</summary>
    [Serializable]
    [StructLayout(LayoutKind.Sequential)]
    [System.Runtime.Serialization.DataContract]
    public partial struct uint2 : IEquatable<uint2>, IComparable<uint2>
    {

        [System.Runtime.Serialization.DataMember(Order = 0)]
        public uint x;

        [System.Runtime.Serialization.DataMember(Order = 1)]
        public uint y;

        public static readonly uint2 zero = new uint2(0u, 0u);

        public uint2(uint x, uint y)
        {
            this.x = x;
            this.y = y;
        }

        public uint2(uint value)
        {
            x = value;
            y = value;
        }

        public uint2(uint2 value)
        {
            x = value.x;
            y = value.y;
        }

        public uint2(uint3 value)
        {
            x = value.x;
            y = value.y;
        }

        public uint2(uint4 value)
        {
            x = value.x;
            y = value.y;
        }

        public uint this[int index]
        {
            get
            {
                if ((uint)index >= Count)
                    throw new ArgumentOutOfRangeException(nameof(index));
                return Unsafe.Add(ref x, index);
            }
            set
            {
                if ((uint)index >= Count)
                    throw new ArgumentOutOfRangeException(nameof(index));
                Unsafe.Add(ref x, index) = value;
            }
        }

        public int Count => 2;

        public bool Equals(uint2 other)
        {
            return x.Equals(other.x) && y.Equals(other.y);
        }

        public override bool Equals(object obj)
        {
            return obj is uint2 other && Equals(other);
        }

        public override int GetHashCode()
        {
            unchecked
            {
                var hash = 17;
                hash = hash * 31 + x.GetHashCode();
                hash = hash * 31 + y.GetHashCode();
                return hash;
            }
        }

        public int CompareTo(uint2 other)
        {
            var xComparison = x.CompareTo(other.x);
            if (xComparison != 0) return xComparison;
            var yComparison = y.CompareTo(other.y);
            if (yComparison != 0) return yComparison;
            return 0;
        }

        public override string ToString()
        {
            return FormattableString.Invariant($"{x}, {y}");
        }

        public static uint2 Parse(string value)
        {
            value = value.Trim();
            if (value.Length >= 2 && value[0] == '[' && value[value.Length - 1] == ']')
                value = value.Substring(1, value.Length - 2);
            var values = value.Split(',');
            if (values.Length != 2)
                throw new FormatException("Expected 2 vector components.");
            return new(uint.Parse(values[0].Trim(), System.Globalization.CultureInfo.InvariantCulture), uint.Parse(values[1].Trim(), System.Globalization.CultureInfo.InvariantCulture));
        }

        public static uint2 Parse(string value, IFormatProvider format)
        {
            value = value.Trim();
            if (value.Length >= 2 && value[0] == '[' && value[value.Length - 1] == ']')
                value = value.Substring(1, value.Length - 2);
            var values = value.Split(',');
            if (values.Length != 2)
                throw new FormatException("Expected 2 vector components.");
            return new(uint.Parse(values[0].Trim(), format), uint.Parse(values[1].Trim(), format));
        }
    }
}
