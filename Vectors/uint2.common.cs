// <auto-generated />
#nullable enable
using System;
using System.Runtime.InteropServices;
using System.Runtime.CompilerServices;

namespace Delta.Maths
{
    public partial struct uint2
    {

        public static uint2 Min(uint2 a, uint2 b)
        {
            return new(Maths.Min(a.x, b.x), Maths.Min(a.y, b.y));
        }

        public static uint2 Min(uint2 a, uint b)
        {
            return new(Maths.Min(a.x, b), Maths.Min(a.y, b));
        }

        public static uint2 Min(uint a, uint2 b)
        {
            return new(Maths.Min(a, b.x), Maths.Min(a, b.y));
        }

        public static uint2 Max(uint2 a, uint2 b)
        {
            return new(Maths.Max(a.x, b.x), Maths.Max(a.y, b.y));
        }

        public static uint2 Max(uint2 a, uint b)
        {
            return new(Maths.Max(a.x, b), Maths.Max(a.y, b));
        }

        public static uint2 Max(uint a, uint2 b)
        {
            return new(Maths.Max(a, b.x), Maths.Max(a, b.y));
        }

        public static uint2 Clamp(uint2 value, uint min, uint max)
        {
            return new(Maths.Clamp(value.x, min, max), Maths.Clamp(value.y, min, max));
        }

        public static uint2 Clamp(uint2 value, uint2 min, uint2 max)
        {
            return new(Maths.Clamp(value.x, min.x, max.x), Maths.Clamp(value.y, min.y, max.y));
        }

        public static uint Sum(uint2 value)
        {
            return value.x + value.y;
        }
    }
}
