// <auto-generated />
#nullable enable
using System;
using System.Runtime.InteropServices;
using System.Runtime.CompilerServices;

namespace KibiHex
{
    public partial struct uint4
    {

        public static uint4 Select(uint4 falseValue, uint4 trueValue, bool4 mask)
        {
            return new(mask.x ? trueValue.x : falseValue.x, mask.y ? trueValue.y : falseValue.y, mask.z ? trueValue.z : falseValue.z, mask.w ? trueValue.w : falseValue.w);
        }

        public static uint4 Select(uint4 falseValue, uint4 trueValue, bool mask)
        {
            return new(mask ? trueValue.x : falseValue.x, mask ? trueValue.y : falseValue.y, mask ? trueValue.z : falseValue.z, mask ? trueValue.w : falseValue.w);
        }

        public static uint4 Select(uint falseValue, uint trueValue, bool4 mask)
        {
            return new(mask.x ? trueValue : falseValue, mask.y ? trueValue : falseValue, mask.z ? trueValue : falseValue, mask.w ? trueValue : falseValue);
        }

        public static bool4 Equal(uint4 a, uint4 b)
        {
            return new(a.x == b.x, a.y == b.y, a.z == b.z, a.w == b.w);
        }

        public static bool4 Equal(uint4 a, uint b)
        {
            return new(a.x == b, a.y == b, a.z == b, a.w == b);
        }

        public static bool4 Equal(uint a, uint4 b)
        {
            return new(a == b.x, a == b.y, a == b.z, a == b.w);
        }

        public static bool4 NotEqual(uint4 a, uint4 b)
        {
            return new(a.x != b.x, a.y != b.y, a.z != b.z, a.w != b.w);
        }

        public static bool4 NotEqual(uint4 a, uint b)
        {
            return new(a.x != b, a.y != b, a.z != b, a.w != b);
        }

        public static bool4 NotEqual(uint a, uint4 b)
        {
            return new(a != b.x, a != b.y, a != b.z, a != b.w);
        }

        public static bool4 LessThan(uint4 a, uint4 b)
        {
            return new(a.x < b.x, a.y < b.y, a.z < b.z, a.w < b.w);
        }

        public static bool4 LessThan(uint4 a, uint b)
        {
            return new(a.x < b, a.y < b, a.z < b, a.w < b);
        }

        public static bool4 LessThan(uint a, uint4 b)
        {
            return new(a < b.x, a < b.y, a < b.z, a < b.w);
        }

        public static bool4 LessThanOrEqual(uint4 a, uint4 b)
        {
            return new(a.x <= b.x, a.y <= b.y, a.z <= b.z, a.w <= b.w);
        }

        public static bool4 LessThanOrEqual(uint4 a, uint b)
        {
            return new(a.x <= b, a.y <= b, a.z <= b, a.w <= b);
        }

        public static bool4 LessThanOrEqual(uint a, uint4 b)
        {
            return new(a <= b.x, a <= b.y, a <= b.z, a <= b.w);
        }

        public static bool4 GreaterThan(uint4 a, uint4 b)
        {
            return new(a.x > b.x, a.y > b.y, a.z > b.z, a.w > b.w);
        }

        public static bool4 GreaterThan(uint4 a, uint b)
        {
            return new(a.x > b, a.y > b, a.z > b, a.w > b);
        }

        public static bool4 GreaterThan(uint a, uint4 b)
        {
            return new(a > b.x, a > b.y, a > b.z, a > b.w);
        }

        public static bool4 GreaterThanOrEqual(uint4 a, uint4 b)
        {
            return new(a.x >= b.x, a.y >= b.y, a.z >= b.z, a.w >= b.w);
        }

        public static bool4 GreaterThanOrEqual(uint4 a, uint b)
        {
            return new(a.x >= b, a.y >= b, a.z >= b, a.w >= b);
        }

        public static bool4 GreaterThanOrEqual(uint a, uint4 b)
        {
            return new(a >= b.x, a >= b.y, a >= b.z, a >= b.w);
        }
    }
}
